id: patterns.recursive
schema_version: "1.2.0"

nodes:
  - id: seed
    type: tool
    tool_name: writer_tool
    dependencies: []

  - id: loop
    type: recursive
    agent_package: test_agents.negotiator
    recursive_sources: [seed]
    convergence_condition: "iteration >= 3"
    max_rounds: 5
    dependencies: [seed]
