"""Legacy developer utilities.

This sub-package collects one-off helper scripts that are useful for
maintainers but should not ship as part of the runtime package.

They are kept *importable* for reference yet live outside the main
`scripts/` directory to avoid polluting the public namespace.
""" 